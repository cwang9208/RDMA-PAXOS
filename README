DARE -- Direct Access REplication
=================================

Brief: A wait-free direct access protocol that uses RDMA features such as QP disconnect and QP timeouts, to ensure highest performance and reliability. This is an RoCE version.

Build DARE
----------
1. Prerequisites
    - libev
    - libconfig (sudo apt-get install libconfig-dev)
    - libdb (sudo apt-get install libdb-dev)
    - libibverbs
2. make

Folder overview
---------------
./src -- source files
./utils 
    - rbtree -- red-black Trees implementation (Linux)
    - libev-4.15.tar.gz 

